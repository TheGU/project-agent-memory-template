# Wiki Index

Catalog of scoped how-it-works pages (progressive disclosure - see what exists before opening
anything). Each line's description is the read-gate trigger: open a page only when your task needs
its depth (read-gate lives in `AGENTS.md`). Descriptions here mirror each page's frontmatter
`description`; update both when a page changes. Format + page template: `README.md`.

<!-- Entry format, one per page, grouped by area:
# <Area>
* [Title](topic.md) - one-line description (from the page's frontmatter `description`).
-->

_No pages yet._
